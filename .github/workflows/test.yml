name: Tests

on:
  push:
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # requirements-dev.txt carries the API test client's dependencies
      # (spec 018, finding 57); without them test discovery cannot import
      # tests/test_reports_api.py.
      - run: pip install -r requirements.txt -r requirements-dev.txt
      - run: python -m unittest discover -s tests

  web:
    # The Python job never needs reports/web/dist: the API tests serve a
    # directory they create. Only this job builds the terminal.
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: reports/web
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: reports/web/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run build
